const resultadoResto = (dividendo, divisor) => {
    console.log(`Resultado do divisao ${Math.floor(dividendo/divisor)}`);
    console.log(`Resto do divisao ${dividendo%divisor}`);

};
resultadoResto(5,2)